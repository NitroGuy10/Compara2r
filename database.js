// Store the following in database

// username, password hash, last used ip hash (for sessions and for deleting bad actors), votes (comma separated line numbers (in base64) of things the user voted as better), flags (comma separated line numbers (in base64) of things the user flagged)

module.exports = {
    login
}

function createAccount (credentials, callback)
{

}

function login (credentials, ipAddress, callback)
{


    callback()
}
